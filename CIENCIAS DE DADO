import pandas as pd

  Dados = { 
     # chave: valor (um array de string) o array é uma lista 
    "nome":["Ana","Jõao","Maria", "pedro", "lucas"],
    # chave: valor(um array de string)
    "cidade": ["londrina","máringa","londrina", Nome, "Cascavel"],
    # chave: valor (um array de inteiro)
    "Idade": [18, 19, 20, 19],
    # chave: (um array de Float (numeros quebrados))
    "Nota":  (8.5, 7.0, 9.0, 6.5, 8.0)
  } # Tipo de dado Objeto chave: valor
  
  mauricio = {
      "nome": "Mauricio", # O valor dessa chave é String
      "cabelo": "Cacheado", # O valor dessa chave é String
      "coe-de-cabelo": "Castanho", # O valor dessa chave é String
      "altura": 1,10 metros, # O valor dessa chave é String
      "aura": 10000000, # O valor dessa chave é Int
      "idade": 17, # O valor dessa chave é Int
      "capital": "R$ 0.00" # O valor dessa chave é String
  }
#sempe que tem parenteses na programaçao e uma funçao
df = pd.DataFrame(dados) # EStou utilizando o pandas para acessar a funçao dataframe

#Exibir o dataFrame
print("===DATAFRAME ===")
print(DF) #Aqui estou mostando TODAS as funçoes do DataFrame
# por que o DF e uma variavel que recebeu TODAS as funções que o DataFrame contem

# estatisticas basicas
print("\=== ESTATISTICAS ===")
# aqui estou utilizando o DF (variavel) ["aqui fica a chave (coluna)"]
print ("media das notas:" , df["notas"].mean()) #mean significa uma media
# Aqui estou utilizando o DF (variavel) ["aqui fica a chave(coluna)"]
print ("maior nota", df["nota"].max()) # Max significa o valor maior
# Aqui estou utilizando o DF (variavel) ["aqui fica a chave(coluna)"]
print

# Resumo estatistico
print(!"\n=== DESCRIBE ===")
print(df.describe()) # Aqui é uma função que da um resumo estatistico para nós

#sempe que tem parenteses na programaçao e uma funçao
df = pd.DataFrame(dados) # EStou utilizando o pandas para acessar a funçao dataframe

#Exibir o dataFrame
print("===DATAFRAME ===")
print(DF) #Aqui estou mostando TODAS as funçoes do DataFrame
# por que o DF e uma variavel que recebeu TODAS as funções que o DataFrame contem

# estatisticas basicas
print("\=== ESTATISTICAS ===")
# aqui estou utilizando o DF (variavel) ["aqui fica a chave (coluna)"]
print ("media das notas:" , df["notas"].mean()) #mean significa uma media
# Aqui estou utilizando o DF (variavel) ["aqui fica a chave(coluna)"]
print ("maior nota", df["nota"].max()) # Max significa o valor maior
# Aqui estou utilizando o DF (variavel) ["aqui fica a chave(coluna)"]
print

# Resumo estatistico
print(!"\n=== DESCRIBE ===")
print(df.describe()) # Aqui é uma função que da um resumo estatistico para nós
#sempe que tem parenteses na programaçao e uma funçao
df = pd.DataFrame(dados) # EStou utilizando o pandas para acessar a funçao dataframe

#Exibir o dataFrame
print("===DATAFRAME ===")
print(DF) #Aqui estou mostando TODAS as funçoes do DataFrame
# por que o DF e uma variavel que recebeu TODAS as funções que o DataFrame contem

# estatisticas basicas
print("\=== ESTATISTICAS ===")
# aqui estou utilizando o DF (variavel) ["aqui fica a chave (coluna)"]
print ("media das notas:" , df["notas"].mean()) #mean significa uma media
# Aqui estou utilizando o DF (variavel) ["aqui fica a chave(coluna)"]
print ("maior nota", df["nota"].max()) # Max significa o valor maior
# Aqui estou utilizando o DF (variavel) ["aqui fica a chave(coluna)"]
print

# Resumo estatistico
print(!"\n=== DESCRIBE ===")
print(df.describe()) # Aqui é uma função que da um resumo estatistico para nós
